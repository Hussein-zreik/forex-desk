# Copy to .env and fill in. Never commit .env.
APP_NAME="Forex Desk API"

# Added in Phase 1+:
# DATABASE_URL=sqlite+aiosqlite:///./forex_desk.db
# ENVIRONMENT=dev   # set to "production" (or anything != dev) in real deploys
# JWT_SECRET=change-me-to-a-long-random-string   # REQUIRED when ENVIRONMENT != dev
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=10080
# TELEGRAM_BOT_TOKEN=
