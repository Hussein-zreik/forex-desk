import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function Dashboard() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Your customizable widget grid lands in Phase 2 — drag, resize, search, and arrange widgets. The live ticker above is already streaming from the backend."
    />
  )
}
